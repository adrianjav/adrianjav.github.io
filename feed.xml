<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://adrianjav.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://adrianjav.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-02-11T17:01:06+00:00</updated><id>https://adrianjav.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Squaring tensor networks and circuits without squaring them</title><link href="https://adrianjav.github.io/blog/2026/osos/" rel="alternate" type="text/html" title="Squaring tensor networks and circuits without squaring them"/><published>2026-02-11T00:00:00+00:00</published><updated>2026-02-11T00:00:00+00:00</updated><id>https://adrianjav.github.io/blog/2026/osos</id><content type="html" xml:base="https://adrianjav.github.io/blog/2026/osos/"><![CDATA[<h2 id="the-quadratic-bottleneck">The quadratic bottleneck</h2> <p>Tensor networks (TNs) and probabilistic circuits (PCs) are powerful tools for tractable probabilistic modeling. They can represent complex distributions while enabling efficient computation of marginals, conditionals, and other probabilistic queries. But there’s a problem when we try to make them more expressive.</p> <p>In short, with both model families we can model probabilities as:</p> \[\newcommand{\dom}{\text{dom}} \newcommand{\bbC}{\mathbb{C}} \newcommand{\uscope}{ {\text{sc}}} \newcommand{\inscope}{ {\text{in}}} \newcommand{\vX}{X} \newcommand{\calO}{\mathcal{O}} p(x) = \frac{\lvert\psi(x)\rvert^2}{Z} = \frac{\psi(x) \cdot \psi^*(x)}{Z}\] <aside><p>If you come from the quantum world, this is pretty similar to <a href="https://en.wikipedia.org/wiki/Born_rule">Born rule</a>.</p></aside> <p>where $\psi(x)$ is a complex-valued function, $\psi^*(x)$ is its conjugate, and $Z = \int \lvert\psi(x)\rvert^2 dx$ is the partition function. This “squared” formulation allows us to use real or complex parameters, making our models strictly more expressive than traditional PCs with only positive parameters, as <a href="https://loreloc.github.io/">Lorenzo</a> showed in AAAI past year <d-cite key="loconte2025sum"></d-cite>.</p> <p><strong>The cost?</strong> Computing \(Z\) or any marginal \(p(y) = \int_z \lvert\ \psi(y,z)\rvert^2 dz\) requires time \(\calO(\lvert c \rvert^2)\) instead of \(\calO(\lvert c\rvert)\), where \(\lvert c\rvert\) is the circuit size. This quadratic overhead happens because we’re effectively evaluating \(\psi \cdot \psi^*\) as a product of two circuits.</p> <aside><p>If you wonder how two PCs can be multiplied together, check this atlas <d-cite key="vergari2021compositional"></d-cite>.</p></aside> <p>In our latest ICLR article <d-cite key="loconte2026square"></d-cite> we show how to <strong>escape this quadratic bottleneck</strong> in squared PCs. The key insight: borrow ideas from both TN canonical forms (orthogonality) and PCs (determinism) to enable linear-time marginalization!</p> <aside><p>We meaning <a href="https://loreloc.github.io/">Lorenzo</a>, <a href="http://nolovedeeplearning.com/">Antonio</a> and <a href="https://adrianjav.github.io">me</a>.</p></aside> <h2 id="background">Background</h2> <h3 id="circuits-and-tensor-networks">Circuits and tensor networks</h3> <p><strong>Circuits</strong> are parameterized computational graphs over a set of variable \(\vX\) encoding a function $c\colon\dom(\vX)\to\bbC$ that hierarchically compose the following types of circuit units, each unit $n$ taken another set of inputs units denoted by $\inscope(n)$:</p> <ul> <li><strong>Input units</strong>: Compute base functions \(f_n(x)\), where $f_n$ is a function over the variable $\uscope(n)={X}\subseteq\vX$, called its <em>scope</em>.</li> <li><strong>Product units</strong>: Multiply their inputs, $\prod_{i\in\inscope(n)} c_i(\uscope(i))$.</li> <li><strong>Sum units</strong>: Compute weighted sums $\sum_{i\in\inscope(n)} w_{i} c_i(\uscope(i))$, where $w_{i}$ are the parameters of the sum unit $n$.</li> </ul> <p><strong>Tensor networks</strong> factorize high-dimensional tensors into products of smaller tensors. The most famous example is the Matrix Product State (MPS) <d-cite key="perez2006mps"></d-cite>:</p> <aside><p>Also known as tensor train decomposition (TNN) <d-cite key="oseledets2011tensor"></d-cite>.</p></aside> \[\psi(x) = \sum_{i_1=1}^R \sum_{i_2=1}^R \cdots \sum_{i_{d-1}=1}^R \psi_1^{i_1}(x_1) \psi_2^{i_1,i_2}(x_2) \cdots \psi_d^{i_{d-1}}(x_d)\] <p>where \(R\) is the rank of the factorization and each \(\psi_j\) is a factor. Computing \(\psi(x)\) naively takes \(\calO(R^d)\) time, but with the right contraction order (left-to-right), it only takes \(\calO(R^2 d)\).</p> <aside><p>That is, as matrix-vector products.</p></aside> <p>The computational graph of sums and products resulting from the TN contraction in a particular ordering <em>turns out to be a circuit</em>.</p> <div class="l-body"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/osos/circuits-and-tensor-networks.svg" sizes="95vw"/> <img src="/assets/img/blog/osos/circuits-and-tensor-networks.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" style=" max-width: 75%; " data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">An MPS tensor network (lower right, Penrose notation) and its representation as a circuit (left). The circuit encodes the left-to-right contraction order.</figcaption> </figure> </div> <p><strong>Efficient marginalization</strong> in PCs—that is, exact and in poly-time wrt. $\calO(\lvert c\rvert)$—is achieved by imposing two <em>structural constraints</em> over them:</p> <ul> <li><strong>Smoothness</strong>: All inputs to a sum unit $n$ depend on the same variables, i.e., $\forall i,j\in\inscope(n)\colon \uscope(i)=\uscope(j)$.</li> <li><strong>Decomposability</strong>: Inputs to a product unit depend on disjoint variable sets, i.e., $\forall i,j\in\inscope(n) : i\neq j\colon \uscope(i)\cap\uscope(j) = \emptyset$.</li> </ul> <p>Together, these ensure we can integrate out any variable in time $\calO(\lvert c\rvert)$.</p> <h3 id="the-born-rule-and-squaring">The Born rule and squaring</h3> <p>To model \(p(x) = \lvert\psi(x)\rvert^2 / Z\), we need to:</p> <ol> <li>Represent \(\psi(x) \cdot \psi^*(x)\) as a circuit.</li> <li>Compute \(Z = \int \lvert\psi(x)\rvert^2 dx\).</li> </ol> <p>The product \(\psi \cdot \psi^*\) can be represented as a decomposable circuit if \(\psi\) is <strong>structured-decomposable</strong> (all products with the same scope factorize it the same way). But this circuit has size \(\calO(\lvert c\rvert^2)\), making marginalization quadratically expensive!</p> <p><strong>Prior solution</strong>: Tensor network <strong>canonical forms</strong> use unitary matrices to simplify marginals. For example, in an MPS with left-canonical form:</p> \[\int \psi_i^{k_1}(x) \psi_i^{k_2}(x)^* dx = \delta_{k_1,k_2}\] <p>This orthonormality makes certain marginals trivial to compute. But canonical forms are TN-specific and don’t extend to general circuits.</p> <h2 id="from-determinism-to-orthogonality">From determinism to orthogonality</h2> <p>Our first key contribution is defining and identifying <strong>orthogonality</strong> as a generalization of <strong>determinism</strong> that enables efficient marginalization in squared circuits.</p> <h3 id="determinism-enables-simplification">Determinism enables simplification</h3> <p>A sum unit \(n\) computing \(c_n(x) = \sum_i w_i c_i(x)\) is <strong>deterministic</strong> if its inputs have disjoint supports: \(\forall i \neq j: \text{supp}(c_i) \cap \text{supp}(c_j) = \emptyset\).</p> <p>When squaring a deterministic sum:</p> \[\lvert c_n(x)\rvert^2 = \left|\sum_i w_i c_i(x)\right|^2 = \sum_i \sum_j w_i w_j^* c_i(x) c_j(x)^* = \sum_i \lvert w_i\rvert ^2 \lvert c_i(x)\rvert ^2\] <p>The cross terms vanish! So determinism reduces squaring from \(\calO(n^2)\) to \(\calO(n)\) terms.</p> <p><strong>But</strong>: Deterministic squared circuits become <em>equivalent to monotonic circuits</em> (only \(\lvert w_i\rvert^2\) appears), losing the expressiveness of complex parameters.</p> <h3 id="orthogonality-the-key-generalization">Orthogonality: The key generalization</h3> <p><strong>Definition (Orthogonality)</strong>: A sum unit \(n\) with scope \(Z\) is orthogonal if:</p> \[\forall i \neq j: \int c_i(z) c_j(z)^* dz = 0\] <p>Unlike determinism, orthogonality allows inputs to have overlapping support: They just need to be orthogonal functions!</p> <p><strong>Example</strong>: Hermite functions or Fourier basis functions are orthogonal but have full support over \(\mathbb{R}\).</p> <div class="l-body-outset"> <div style="display: inline-flex; justify-content: space-evenly; align-items: center; column-gap: 5%; width: 100%;"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/osos/disjsupp.svg" sizes="95vw"/> <img src="/assets/img/blog/osos/disjsupp.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/osos/orthogonal.svg" sizes="95vw"/> <img src="/assets/img/blog/osos/orthogonal.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="caption"> Determinism requires disjoint supports (left). Orthogonality allows overlapping supports as long as functions are orthogonal (right). </div> </div> <p><strong>Theorem 1</strong> (Simplified): If circuit \(c\) is smooth, decomposable, and orthogonal, then computing \(Z = \int \lvert c(x)\rvert^2 dx\) takes time \(\calO(\lvert c\rvert)\).</p> <p><strong>Proof sketch</strong>: When integrating \(\lvert c_n(x)\rvert^2 = \sum_i \sum_j w_i w_j^* c_i(x) c_j(x)^*\), orthogonality makes cross terms vanish after integration, leaving only \(\sum_i \lvert w_i\rvert ^2 \int \lvert c_i(z)\rvert ^2 dz\).</p> <p>Crucially, orthogonality preserves complex parameters in the distribution, unlike determinism!</p> <h3 id="unlocking-non-structured-decomposable-circuits">Unlocking non-structured-decomposable circuits</h3> <p>Orthogonality works even for <strong>non-structured-decomposable</strong> circuits—those encoding multiple variable partitionings. This is significant because:</p> <ul> <li>Structured-decomposable circuits (like MPSs and TTNs) can be exponentially less expressive.</li> <li>Previous methods required structured decomposability for tractable squaring.</li> <li>Orthogonality bypasses this limitation.</li> </ul> <h2 id="building-orthogonal-circuits">Building orthogonal circuits</h2> <p>How do we construct circuits that satisfy orthogonality? The paper introduces <strong>regular orthogonality</strong>.</p> <h3 id="regular-orthogonality">Regular orthogonality</h3> <p><strong>Basis decomposability</strong>: A sum unit \(n\) is basis decomposable if there exists a variable \(\vX\) such that inputs depend on disjoint sets of input functions over \(\vX\).</p> <p><strong>Regular orthogonality</strong>: A circuit is regular orthogonal if:</p> <ol> <li>It’s basis decomposable.</li> <li>All input functions over the same variable are orthogonal.</li> </ol> <p><strong>Theorem 2</strong>: Regular orthogonality \(\Rightarrow\) Orthogonality.</p> <p>This gives us a practical recipe: choose orthogonal input functions and ensure sum units split their dependencies appropriately.</p> <h3 id="choices-for-orthogonal-functions">Choices for orthogonal functions</h3> <p><strong>Discrete variables</strong>:</p> <ul> <li><strong>Kronecker deltas</strong> \(\{\delta_{xk}\}_k\).</li> <li><strong>Orthogonal embeddings</strong> where \(p(x) = W W^*\) with $W$ row-unitary.</li> </ul> <p><strong>Continuous variables</strong>:</p> <ul> <li><strong>Fourier basis</strong>: \(f_k(x) \propto e^{2\pi i kx / P}\) (periodic functions).</li> <li><strong>Hermite functions</strong>: Orthonormal basis for \(L^2(\mathbb{R})\).</li> <li><strong>Orthogonal polynomials</strong>: Legendre, Chebyshev, etc.</li> </ul> <p>The choice depends on your domain and the functions you want to approximate.</p> <h2 id="scaling-up-unitarity-for-tensorized-circuits">Scaling up: Unitarity for tensorized circuits</h2> <p>Regular orthogonality works well for circuits where each sum connects to different input functions. But modern circuits often have <strong>dense layers</strong> where multiple sums share inputs—these aren’t basis decomposable!</p> <div class="row mt-3" style="display: inline-flex; justify-content: space-evenly; align-items: center; column-gap: 5%; width: 100%;"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/osos/decomposability.svg" sizes="95vw"/> <img src="/assets/img/blog/osos/decomposability.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" style=" max-width: 45%; " data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Basis decomposable: each sum connects to different inputs.</figcaption> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/osos/basis-decomposability-gadgets.svg" sizes="95vw"/> <img src="/assets/img/blog/osos/basis-decomposability-gadgets.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" style=" max-width: 50%; " data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">NOT basis decomposable: sums share input connections</figcaption> </figure> </div> </div> <p>The solution: <strong>unitarity</strong>, which defines conditions at the <em>layer level</em>.</p> <h3 id="tensorized-circuits">Tensorized circuits</h3> <p>A <strong>tensorized circuit</strong> groups units into layers:</p> <ul> <li><strong>Input layers</strong>: Output \(K\)-dimensional vectors of functions.</li> <li><strong>Product layers</strong>: Element-wise (Hadamard) or Kronecker products.</li> <li><strong>Sum layers</strong>: Matrix-vector products \(W \cdot \ell\).</li> </ul> <p>This is just a convenient representation that allows for efficient GPU usage and over-parametrization. In general, it is equivalent to a regular circuit.</p> <h3 id="unitarity-conditions">Unitarity conditions</h3> <p>A tensorized circuit is <strong>unitary</strong> if it satisfies:</p> <p><strong>(U1) Orthonormal inputs</strong>: Each input layer uses orthonormal functions, i.e.,</p> \[\int \ell(x) \otimes \ell(x)^* dx = I_K \;.\] <p><strong>(U2) Separated dependencies</strong>: Each sum layer’s inputs depend on different input layers (for at least one variable).</p> <p><strong>(U3) Semi-unitary weights</strong>: Each sum layer’s weight matrix \(W\) satisfies \(WW^\dagger = I\).</p> <p><strong>Theorem 3</strong>: If \(c\) is unitary, then:</p> <ol> <li>\(c\) is orthogonal.</li> <li>\(Z = \int \lvert c(x)\rvert^2 dx = 1\) (already normalized!).</li> </ol> <p><strong>Proof sketch</strong>: By induction on layers, show \(\int \ell(z) \otimes \ell(z)^* dz = I_K\) for every layer. The semi-unitary property ensures: \(\int (W\ell) \otimes (W\ell)^* dz = (W \otimes W^*) I_K = WW^\dagger = I\).</p> <h3 id="connection-to-tns">Connection to TNs</h3> <p>Unitarity generalizes the <strong>upper-canonical form</strong> of tree tensor networks (TTNs). TTNs in upper-canonical form have:</p> <ul> <li>Orthonormal factors at leaves.</li> <li>Each inner tensor is an isometry.</li> </ul> <p>When represented as a tensorized circuit, these become exactly conditions (U1) and (U3)!</p> <p>But unitarity extends to <strong>non-TN structures</strong>—circuits that encode multiple variable partitionings simultaneously, which no TN can represent.</p> <div class="l-body"> <div style="display: inline-flex; justify-content: space-evenly; align-items: center; column-gap: 5%; width: 100%;"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/osos/figb1.svg" sizes="95vw"/> <img src="/assets/img/blog/osos/figb1.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" style=" max-width: 78%; " data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Tree tensor network (TTN) represented as a tensorized circuit.</figcaption> </figure> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/osos/fig4.svg" sizes="95vw"/> <img src="/assets/img/blog/osos/fig4.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Tensorized circuit with no corresponding TTN.</figcaption> </figure> </div> </div> <h2 id="efficient-marginalization">Efficient marginalization</h2> <p>Even with unitarity ensuring \(Z = 1\), we still need to compute marginals \(p(y) = \int \lvert c(y,z)\rvert^2 dz\). The naive approach: materialize the squared circuit (size \(\calO(L^2 S_{\max}^2)\)) and marginalize (time \(\calO(L^2 S_{\max}^2)\)).</p> <p>We present an algorithm that achieves:</p> \[\calO(\lvert\phi_Y \setminus \phi_Z\rvert S_{\max} + \lvert\phi_Y \cap \phi_Z\rvert S_{\max}^2)\] <p>where \(\phi_Y\) = layers depending on \(Y\), \(\phi_Z\) = layers depending on \(Z\).</p> <p><strong>Key insights</strong>:</p> <ol> <li>Layers depending only on \(Z\) integrate to identity matrices (from unitarity) — don’t evaluate them!</li> <li>Layers depending only on \(Y\) don’t need squaring — evaluate once: \(\ell(y) \otimes \ell(y)^*\)</li> <li>Only layers depending on both \(Y\) and \(Z\) need full squaring</li> </ol> <p>This requires strengthening <strong>(U2)</strong>:</p> <ul> <li><strong>(U4)</strong>: Inputs must have disjoint dependencies for <strong>all</strong> variables (not just one).</li> </ul> <p><strong>Best case</strong>: \(\calO(\lvert\phi_Y \setminus \phi_Z\rvert S_{\max})\) when only a few boundary layers depend on both \(Y\) and \(Z\).</p> <p><strong>Example</strong>: Marginalizing the left half of an image. Most layers depend entirely on either left or right pixels. Only layers near the output depend on both, so \(\lvert\phi_Y \cap \phi_Z\rvert = \calO(1)\) while \(\lvert\phi_Y\rvert = \calO(L)\).</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">marginalize</span><span class="p">(</span><span class="n">layer</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">Z</span><span class="p">):</span>
    <span class="k">if</span> <span class="nf">scope</span><span class="p">(</span><span class="n">layer</span><span class="p">)</span> <span class="err">⊆</span> <span class="n">Z</span><span class="p">:</span>
        <span class="c1"># Only marginalized variables
</span>        <span class="k">return</span> <span class="n">I</span>  <span class="c1"># From unitarity!
</span>
    <span class="k">if</span> <span class="nf">scope</span><span class="p">(</span><span class="n">layer</span><span class="p">)</span> <span class="err">∩</span> <span class="n">Z</span> <span class="o">==</span> <span class="err">∅</span><span class="p">:</span>
        <span class="c1"># Only kept variables
</span>        <span class="n">r</span> <span class="o">=</span> <span class="nf">evaluate</span><span class="p">(</span><span class="n">layer</span><span class="p">,</span> <span class="n">y</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">r</span> <span class="err">⊗</span> <span class="n">r</span><span class="p">.</span><span class="n">T</span><span class="p">.</span><span class="nf">conj</span><span class="p">()</span>

    <span class="c1"># Both kept and marginalized
</span>    <span class="k">if</span> <span class="n">layer</span> <span class="ow">is</span> <span class="nb">sum</span><span class="p">:</span>
        <span class="n">results</span> <span class="o">=</span> <span class="p">[</span><span class="nf">marginalize</span><span class="p">(</span><span class="nb">input</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">Z</span><span class="p">)</span> <span class="k">for</span> <span class="nb">input</span> <span class="ow">in</span> <span class="n">layer</span><span class="p">.</span><span class="n">inputs</span><span class="p">]</span>
        <span class="c1"># Crucially: only diagonal terms survive (U4)
</span>        <span class="k">return</span> <span class="nf">sum</span><span class="p">(</span><span class="n">W</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">@</span> <span class="n">R</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">@</span> <span class="n">W</span><span class="p">[</span><span class="n">i</span><span class="p">].</span><span class="n">T</span><span class="p">.</span><span class="nf">conj</span><span class="p">()</span> <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">R</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">results</span><span class="p">))</span>

    <span class="k">if</span> <span class="n">layer</span> <span class="ow">is</span> <span class="n">product</span><span class="p">:</span>
        <span class="n">r1</span> <span class="o">=</span> <span class="nf">marginalize</span><span class="p">(</span><span class="n">layer</span><span class="p">.</span><span class="n">input1</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">Z</span> <span class="err">∩</span> <span class="nf">scope</span><span class="p">(</span><span class="n">layer</span><span class="p">.</span><span class="n">input1</span><span class="p">))</span>
        <span class="n">r2</span> <span class="o">=</span> <span class="nf">marginalize</span><span class="p">(</span><span class="n">layer</span><span class="p">.</span><span class="n">input2</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">Z</span> <span class="err">∩</span> <span class="nf">scope</span><span class="p">(</span><span class="n">layer</span><span class="p">.</span><span class="n">input2</span><span class="p">))</span>
        <span class="k">return</span> <span class="n">r1</span> <span class="err">⊙</span> <span class="n">r2</span>  <span class="c1"># or permuted Kronecker
</span></code></pre></div></div> <h2 id="experimental-results">Experimental results</h2> <p>The experiments validate three key questions:</p> <h3 id="rq1-do-unitary-circuits-scale-better">RQ1: Do unitary circuits scale better?</h3> <div class="l-body"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/osos/nunits.svg" sizes="95vw"/> <img src="/assets/img/blog/osos/nunits.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" style=" max-width: 75%; " data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Time and memory per training iteration vs. parameter count. Unitary circuits ($\perp_\bbC^2$) are consistently faster and more memory-efficient.</figcaption> </figure> </div> <p><strong>Setup</strong>: Compare \(\pm^2_C\) (baseline squared PCs) vs \(\perp^2_C\) (squared unitary PCs) with increasing model size.</p> <p><strong>Results</strong>:</p> <ul> <li>At 357M parameters: \(\perp^2_C\) uses 12 GiB vs 18 GiB (33% reduction).</li> <li>Training iteration: 0.29ms vs 0.52ms (44% faster).</li> </ul> <p><strong>Why?</strong> Not materializing the squared circuit saves both memory and compute.</p> <h3 id="rq2-can-we-train-unitary-circuits-without-losing-performance">RQ2: Can we train unitary circuits without losing performance?</h3> <div class="l-body"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog/osos/bpd-curves-both.svg" sizes="95vw"/> <img src="/assets/img/blog/osos/bpd-curves-both.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" style=" max-width: 75%; " data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Test bits-per-dimension on MNIST and FashionMNIST. Unitary circuits (⊥²C) match baseline performance across scales.</figcaption> </figure> </div> <p><strong>Setup</strong>: Train on MNIST and FashionMNIST, measure bits-per-dimension (lower = better).</p> <p><strong>Results</strong>: \(\perp^2_C\) with Kronecker layers matches \(\pm^2_C\) with Hadamard layers across all model sizes.</p> <p><strong>Key</strong>: Adapted the LandingSGD optimizer <d-cite key="ablin2022fast"></d-cite> (for orthogonal constraints) by:</p> <ul> <li>Projecting back to Stiefel manifold when drifting too much.</li> <li>Using VectorAdam-based gradients <d-cite key="ling2022vectoradam"></d-cite>.</li> </ul> <p>We called the final variant <em>LandingPC</em>.</p> <h3 id="rq3-do-non-structured-circuits-work">RQ3: Do non-structured circuits work?</h3> <p><strong>Setup</strong>: Train a \(\perp^2_C\) circuit that encodes multiple variable partitionings (non-structured-decomposable).</p> <p><strong>Results</strong>: Competitive with structured circuits, especially at large scales, though harder to train.</p> <p><strong>Significance</strong>: First demonstration of tractably training non-structured squared PCs! These can be exponentially more expressive than structured ones.</p> <h2 id="conclusion">Conclusion</h2> <p>This work bridges tensor networks and probabilistic circuits by showing that <strong>orthogonality</strong> unifies two seemingly different ideas:</p> <ul> <li><strong>Canonical forms</strong> in TNs (unitary matrices).</li> <li><strong>Determinism</strong> in circuits (disjoint supports).</li> </ul> <h3 id="key-takeaways">Key Takeaways</h3> <p><strong>Theoretical</strong>:</p> <ol> <li>Orthogonality enables \(\calO(\lvert c\rvert)\) marginalization in squared circuits.</li> <li>Unitarity provides practical layer-level conditions.</li> <li>Works for non-structured circuits (beyond what TNs can represent).</li> </ol> <p><strong>Practical</strong>:</p> <ol> <li>Unitary circuits are faster and use less memory.</li> <li>No performance loss with proper optimization (LandingPC).</li> <li>Enables new architectures (Kronecker layers, non-structured).</li> </ol>]]></content><author><name>Lorenzo Loconte</name></author><category term="papers"/><category term="probabilistic-circuits"/><summary type="html"><![CDATA[or how to effectively exploit orthogonality constraints]]></summary></entry></feed>